{
  "name": "kikitori",
  "version": "0.1.0",
  "description": "Whisper + Ollama powered transcription CLI",
  "type": "module",
  "bin": {
    "kikitori": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node",
    "dev": "bun run ./src/index.ts",
    "start": "bun run ./dist/index.js"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "ollama": "^0.6.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
